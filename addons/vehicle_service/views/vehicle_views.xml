<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Vehicle List View -->
    <record id="view_vehicle_list" model="ir.ui.view">
        <field name="name">vehicle.vehicle.list</field>
        <field name="model">vehicle.vehicle</field>
        <field name="arch" type="xml">
            <tree>
                <field name="registration_number"/>
                <field name="manufacturer_id"/>
                <field name="model_id"/>
                <field name="owner_id"/>
                <field name="manufacturing_year"/>
            </tree>
        </field>
    </record>

    <!-- Vehicle Form View -->
    <record id="view_vehicle_form" model="ir.ui.view">
        <field name="name">vehicle.vehicle.form</field>
        <field name="model">vehicle.vehicle</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button
                        name="%(action_vehicle_checkin_wizard)d"
                        type="action"
                        string="Check-In"
                        class="btn-primary"
                        context="{'active_id': id}"
                        invisible="not id"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button
                            name="action_view_service_orders"
                            type="object"
                            class="oe_stat_button"
                            icon="fa-wrench">
                            <field
                                name="service_order_count"
                                widget="statinfo"
                                string="Service Orders"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <label for="registration_number"/>
                        <h1>
                            <field name="registration_number"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="manufacturer_id"/>
                            <field
                                name="model_id"
                                domain="[('manufacturer_id', '=', manufacturer_id)]"/>
                            <field name="manufacturing_year"/>
                            <field name="vehicle_age" readonly="1"/>
                        </group>
                        <group>
                            <field name="owner_id"/>
                            <field name="owner_mobile" readonly="1"/>
                            <field name="owner_email" readonly="1"/>
                            <field name="active"/>
                        </group>
                    </group>
                    <group string="Cost Details">
                        <group>
                            <field name="parts_cost"/>
                            <field name="labour_cost"/>
                        </group>
                        <group>
                            <field name="total_cost" readonly="1"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="History">
                            <group string="Latest Completed Service">
                                <field
                                    name="last_service_order_id"
                                    readonly="1"/>
                            </group>
                            <group>
                                <button
                                    name="action_view_completed_service_history"
                                    type="object"
                                    string="View Complete History"
                                    class="btn-primary"/>
                                <button
                                    name="action_new_service_order"
                                    type="object"
                                    string="Create New Service Order"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" groups="base.group_user"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Vehicle Search View -->
    <record id="view_vehicle_search" model="ir.ui.view">
        <field name="name">vehicle.vehicle.search</field>
        <field name="model">vehicle.vehicle</field>
        <field name="arch" type="xml">
            <search>
                <field name="registration_number"/>
                <field name="owner_id"/>
                <field name="manufacturer_id"/>
                <field name="model_id"/>
                <separator/>
                <filter
                    name="active"
                    string="Active"
                    domain="[('active', '=', True)]"/>
                <filter
                    name="inactive"
                    string="Archived"
                    domain="[('active', '=', False)]"/>
                <separator/>
                <group expand="0" string="Group By">
                    <filter
                        string="Owner"
                        name="group_owner"
                        context="{'group_by':'owner_id'}"/>
                    <filter
                        string="Manufacturer"
                        name="group_manufacturer"
                        context="{'group_by':'manufacturer_id'}"/>
                    <filter
                        string="Model"
                        name="group_model"
                        context="{'group_by':'model_id'}"/>
                    <filter
                        string="Manufacturing Year"
                        name="group_manufacturing_year"
                        context="{'group_by':'manufacturing_year'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Vehicle Action -->
    <record id="action_vehicle" model="ir.actions.act_window">
        <field name="name">Vehicle</field>
        <field name="res_model">vehicle.vehicle</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_vehicle_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first vehicle.
            </p>
            <p>
                Manage customer vehicles
                and their service information.
            </p>
        </field>
    </record>

</odoo>
